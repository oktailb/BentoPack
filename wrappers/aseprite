#!/usr/bin/env bash
# Transparent batch wrapper for Aseprite using BentoPack CLI
if command -v bentopack-cli >/dev/null 2>&1; then
  exec bentopack-cli --flavor aseprite "$@"
else
  exec bentopack-cli --flavor aseprite "$@"
fi
